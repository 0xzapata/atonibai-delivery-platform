{
  "name": "kaoncdo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:web": "npm run dev --workspace=@kaoncdo/web",
    "dev:api": "npm run dev --workspace=@kaoncdo/api",
    "build": "npm run build --workspaces --if-present"
  }
}
